apiVersion: v1
kind: Secret
metadata:
  namespace: ns-simplebankapp
  name: simplebankapp-secrets
type: Opaque
stringData:
  # ⚠️ Replace with your actual MySQL database password
  # For production, use kubectl create secret instead:
  #   kubectl create secret generic simplebankapp-secrets \
  #     --from-literal=db-password=YOUR_DB_PASSWORD \
  #     -n ns-simplebankapp
  db-password: "Test@123"
