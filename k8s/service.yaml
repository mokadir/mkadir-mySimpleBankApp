apiVersion: v1
kind: Service
metadata:
  namespace: ns-simplebankapp
  name: simplebankapp-service
  labels:
    app: simplebankapp
spec:
  selector:
    app: simplebankapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30024
  type: NodePort
